/**
 *
 * @param {String} Rn_Binary n start from 0
 * @param {String} Knpp_Binary n + 1
 *
 * @example feistel(R0, K1) {}
 */
export default function feistel(Rn_Binary: string, Knpp_Binary: string) {
  // Mở rộng nửa phải
}
